1、生成代码前先阅读/api-docs文件夹下的相关文档和规则，并按rules.md的规则进行代码生成。
2、生成代码后，更新api-docs文件夹下的相关文档，包括接口文档、参数文档、返回值文档等。
3、当前开发环境没有安装任何http服务器，需要启动游戏的时候请提示用户通过Live Server插件启动游戏。
4、PowerShell5不支持&&运算符，需要使用分号分隔多个命令。
5、禁止使用nodejs、npm、python等命令。