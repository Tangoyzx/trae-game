class Player extends Entity {
    constructor() {
        super();
    }

    initialize() {
        // 初始化逻辑
    }

    update(deltaTime) {
        super.update(deltaTime);
    }

    render(ctx) {
        super.render(ctx);
    }
}